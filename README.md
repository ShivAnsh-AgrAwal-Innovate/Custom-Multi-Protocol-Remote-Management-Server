# Custom-Multi-Protocol-Remote-Management-Server
Custom remote management server built from scratch in C — raw TCP, binary protocol, epoll multiplexing, shell execution, and bidirectional file transfer. No HTTP, no libraries, no abstractions.
